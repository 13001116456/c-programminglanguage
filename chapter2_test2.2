#include <stdio.h>

/*-----while循环和enum枚举常量模拟for循环&&与||判断----------c-programming2-2----------------------*/
void heart_pri_t(int lim)
{
	enum flag {NO ,YES};
	enum flag mark = YES;   //在上述枚举值中取值
	int i = 0;
	char ch[LIM_LEN],c;

	while(mark == YES)
	{
		if( i >= lim - 1)  //设置相反的条件置mark相反
			mark = NO;
		else if((c = getchar()) == '\n')
			mark = NO;
		else if(c == EOF)
			mark = NO;
		else
		{
			ch[i] = c;
			i++;
		}
	}
	ch[lim - 1] = '\0';
	puts(ch); //采取puts打印数组不出现溢出打印乱码需要在数组结束时候加上结束符号\0
	i = 0;   //循环方式打印不会越界打印出现乱码
	while(i < lim)
	{
		printf("%c",ch[i]);
		i++;
	}
}
/*-----while循环和enum枚举常量模拟for循环&&与||判断----------over-----------------*/
